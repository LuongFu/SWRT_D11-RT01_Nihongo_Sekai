@model IEnumerable<JapaneseLearningPlatform.Data.ViewModels.ClassroomTemplateVM>

@{
    ViewData["Title"] = "My Classroom Templates";
}
<div class="container mt-4">

<h2 class="mb-4">My Classroom Templates</h2>

<div class="mb-3 text-end">
    <a asp-action="Create" class="btn btn-primary">
        <i class="bi bi-plus-circle me-1"></i> Create a new Classroom Template
    </a>
</div>

@if (!Model.Any())
{
    <div class="alert alert-warning text-center">
        <i class="bi bi-exclamation-circle me-2"></i> No Templates yet.
    </div>
}
else
{
    <table class="table table-bordered table-striped align-middle">
        <thead class="table-light">
            <tr>
                <th style="width:100px">Thumbnail</th>
                <th>Title</th>
                <th>Level</th>
                <th style="width:170px" class="text-center">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @if (!string.IsNullOrEmpty(item.ImageURL))
                        {
                            <img src="@item.ImageURL" class="img-thumbnail" style="width:80px; height:80px; object-fit:cover;" />
                        }
                        else
                        {
                            <div class="bg-secondary text-white d-flex align-items-center justify-content-center" style="width:80px; height:80px;">
                                <i class="bi bi-image" style="font-size:1.5rem;"></i>
                            </div>
                        }
                    </td>
                    <td>@item.Title</td>
                    <td>@item.LanguageLevel</td>
                    <td class="text-center">
                        <a asp-action="Detail" asp-route-id="@item.Id" class="btn btn-sm btn-info me-1" title="Xem chi tiết">
                            <i class="bi bi-eye"></i>
                        </a>
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning me-1" title="Chỉnh sửa">
                            <i class="bi bi-pencil-square"></i>
                        </a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger" title="Xóa">
                            <i class="bi bi-trash"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
</div> <!-- End container -->
