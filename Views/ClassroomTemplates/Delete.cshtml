@using JapaneseLearningPlatform.Data.ViewModels
@model ClassroomTemplateVM

@{
    ViewData["Title"] = "Delete Template";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card border-danger shadow">
                <div class="card-body">
                    <h3 class="text-danger mb-4 text-center">
                        <i class="bi bi-trash3-fill me-2"></i>Confirm Delete
                    </h3>

                    <dl class="row">
                        <dt class="col-sm-4 fw-semibold">Title:</dt>
                        <dd class="col-sm-8">@Model.Title</dd>

                        <dt class="col-sm-4 fw-semibold">Description:</dt>
                        <dd class="col-sm-8">@Model.Description</dd>

                        <dt class="col-sm-4 fw-semibold">Language Level:</dt>
                        <dd class="col-sm-8">@Model.LanguageLevel</dd>
                    </dl>
                    <div class="alert alert-warning mt-4">
                        <i class="bi bi-exclamation-triangle-fill me-2"></i>
                        This action cannot be undone. Are you sure you want to delete this classroom template?
                    </div>

                    <form asp-action="Delete" method="post" class="mt-4 d-flex justify-content-between">
                        <input type="hidden" asp-for="Id" />
                        <a href="javascript:history.back()" class="btn btn-outline-secondary">
                            <i class="bi bi-arrow-left"></i> Cancel
                        </a>
                        <button type="submit" class="btn btn-danger">
                            <i class="bi bi-trash-fill"></i> Delete
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
